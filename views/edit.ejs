<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Note Editor</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
      background-color: #f4f6f8;
      height: 100vh;
      display: flex;
      flex-direction: column;
    }

    header {
      background-color: #ffffff;
      padding: 20px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
      text-align: center;
      font-size: 24px;
      font-weight: bold;
    }

    main {
      flex: 1;
      padding: 30px 40px;
    }

    textarea {
      width: 100%;
      height: 80%;
      padding: 15px;
      font-size: 16px;
      border: 1px solid #ccc;
      border-radius: 6px;
      resize: none;
    }

    .form-buttons {
      margin-top: 15px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    input[type="submit"] {
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    input[type="submit"]:hover {
      background-color: #45a049;
    }

    a {
      text-decoration: none;
      color: #4CAF50;
      font-weight: bold;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <header>
    <%= filename.replace('.txt', '') %>
  </header>

  <main>
    <form action="/updateFile/<%= id %>/" method="post">
      <input type="hidden" name="title" value="<%= filename.replace('.txt', '') %>">

      <textarea name="description" placeholder="Revise your thoughts"><%= data %></textarea>

      <div class="form-buttons">
        <input type="submit" value="Redress Task">
        <a href="/">Don't wanna change your mind?</a>
      </div>
    </form>
  </main>
</body>
</html>
